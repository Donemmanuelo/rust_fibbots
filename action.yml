  name: Rust CI Input
  inputs:
    max_threshold:   # id of max_threshold
      description: 'Maximum threshold value'
      required: false
      default: 100
    enable_fib:  # id of enable_fib
      description: 'Enable fib value'
      required: false  # Change to false if not required
      default: true
  runs:
    using: 'docker'
    image: 'Dockerfile'
    args:
      - ${{ inputs.max_threshold }}
      - ${{ inputs.enable_fib }}
      
<<<<<<< HEAD

=======
  name: Run Fibbot 
   run: |
    export PR_NUMBER="${{ inputs.pr_number }}"
    export GITHUB_TOKEN="${{ inputs.token }}"
    shell: bash
>>>>>>> 6218b77606fd84a86416ac27211e4872cd2b5c15


    
